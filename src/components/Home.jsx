import LoginTemplate from '../templates/LoginTemplate'
import { showToast } from '../features/toast/toastSlice';
showToast

const Home = () => {
  
  return (
    <LoginTemplate>
      
    </LoginTemplate>
  );
};

export default Home;
